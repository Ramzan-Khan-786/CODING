#include<iostream>

using namespace std;
 
int main(){
    char a='a';
    if(((a>=97) && (a<=122)) || ((a>=65) && (a<=90))){
        cout <<" alphabet";
    }
    else{
        cout<<" not alphabet";
    }
    
 return 0;
}

